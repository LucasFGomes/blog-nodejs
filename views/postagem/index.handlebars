<div class="card">
    <div class="card-body">
        <h2>{{postagem.titulo}}</h2>
        <small>{{postagem.categoria.nome}}</small> <br>
        <small>Data de publicação: {{postagem.data}}</small>
        <hr>
        <h5>{{postagem.descricao}}</h5>
        <p>{{postagem.conteudo}}</p>
    </div>
</div>